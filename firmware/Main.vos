/*
Vos Symbols:
+ - * u* / u/ % /% u/% & | ^ > < = != >= <= u> u< u>= u<=
pop nip dup swap rot over
@ ! b@ sb@ b! h@ sh@ h!
auto const var "String"

if (cond) elseif (cond) else loop (cond) do break
struct alloc
*/

.include_asm "Startup.s"
.include "Sources.hvos"

fn main ()
    /* Preform System Diagnostics */
    RunSystemDiagnostics

    if 2 dup 0 = do
        /* 1 */
    end elseif dup 1 = do
        /* 2 */
    end else
        /* 3 */
    end
end